spring.application.name=Authentication

server.port=${port:8011}
# Import the properties from the .env file
#spring.config.import=classpath:application.properties,classpath:.env

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=evidendev@gmail.com
spring.mail.password=noqg bdfa zaii zupp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.smtp.ssl.enable=true

# Actuator Web port
management.server.port=8111
management.endpoint.health.show-details=always

#spring.datasource.url=jdbc:mysql://authentication-service-db:3306/authentication_db
#spring.datasource.username=root
#spring.datasource.password=root
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/gamemaster_db
spring.datasource.username=myuser
spring.datasource.password=mypassword
#When database is put on update it will keep all lobby information which currently cant be assigned a finished status so you cant filter them.
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql=true
springdoc.api-docs.resolve-schema-properties=true
springdoc.swagger-ui.path=/swagger

# JWT
security.jwt.secret-key=dae0c4ec944d3d77710050cd128ae20bd34c1f05198628de640d4eb26d49faed
security.jwt.expiration-time=3600000

# Kafka
spring.kafka.bootstrap-servers=localhost:29092
spring.cloud.stream.kafka.binder.configuration.key.serializer=org.apache.kafka.common.serialization.UUIDSerializer
spring.cloud.stream.kafka.binder.configuration.key.deserializer=org.apache.kafka.common.serialization.UUIDDeserializer

# Logs formatted SQL (optional, makes SQL more readable)
spring.jpa.properties.hibernate.format_sql=true

# Logs the SQL statement with parameters
spring.jpa.properties.hibernate.use_sql_comments=true
spring.jpa.properties.hibernate.generate_statistics=true

# Set logging level to DEBUG for parameter values
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
