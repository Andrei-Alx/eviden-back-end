apiVersion: apps/v1
kind: Deployment
metadata:
  name: eviden-gameappbff-service-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eviden-gameappbff-service-deployment
  template:
    metadata:
      labels:
        app: eviden-gameappbff-service-deployment
    spec:
      containers:
        - name: eviden-gameappbff-service-app
          image: i486205/eviden-gamebff-service:development
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://eviden-gameappbff-service-db:3306/gameappbff_db"
            - name: SPRING_KAFKA_BOOTSTRAP-SERVERS
              value: "my-cluster-kafka-bootstrap:9092"
            - name: SPRING_JPA_HIBERNATE_DDL-AUTO
              value: "update"
          ports:
            - containerPort: 8004
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8104
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8104
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: gameappbff-service-service
spec:
  selector:
    app: eviden-gameappbff-service-deployment
  ports:
    - protocol: TCP
      port: 8004
      targetPort: 8004
