apiVersion: apps/v1
kind: Deployment
metadata:
  name: eviden-round-service-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eviden-round-service
  template:
    metadata:
      labels:
        app: eviden-round-service
    spec:
      containers:
        - name: eviden-round-service
          image: europe-west4-docker.pkg.dev/vocal-invention-443612-d9/docker-images/eviden-back-end-round-service:kubernetes-test
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://172.16.100.14:3306/round_db?allowPublicKeyRetrieval=true"
            - name: SPRING_KAFKA_BOOTSTRAP-SERVERS
              value: "my-cluster-kafka-bootstrap:9092"
            - name: SPRING_JPA_HIBERNATE_DDL-AUTO
              value: "update"
          ports:
            - containerPort: 8002
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: eviden-round-service
spec:
  type: ClusterIP
  selector:
    app: eviden-round-service
  ports:
    - port: 8002
      targetPort: 8002
